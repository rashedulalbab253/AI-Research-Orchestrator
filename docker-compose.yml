version: '3.8'

services:
  research-orchestrator:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - PORT=8000
    restart: always

networks:
  default:
    name: research-orchestrator-network
